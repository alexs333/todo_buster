require 'spec_helper'

describe TodoBuster do
  it 'has a version number' do
    expect(TodoBuster::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
